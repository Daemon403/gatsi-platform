import { dateTime, getActiveUser, makeId, money, nextStatus, orderBalance, orderPaid, orderSubtotal, orderTotal, shortDate, statusLabels, statusSequence, type PaymentMethod } from '@gatsi/domain';
import { ArrowLeft, ArrowRight, CalendarDays, Check, CreditCard, FileText, MapPin, Package2, Printer, Shirt, UserRound } from 'lucide-react';
import { useState } from 'react';
import { Link, useParams } from 'react-router-dom';
import { Button, Card, Empty, FormField, PageTitle, StatusPill } from '../components/ui';
import { useAppStore } from '../store/AppStore';

const methods: PaymentMethod[] = ['cash', 'ecocash', 'card', 'bank_transfer'];

export function OrderDetailPage() {
  const { orderId } = useParams();
  const { state, dispatch } = useAppStore();
  const user = getActiveUser(state)!;
  const order = state.orders.find((item) => item.id === orderId);
  const [amount, setAmount] = useState('');
  const [method, setMethod] = useState<PaymentMethod>('cash');
  if (!order) return <Empty warning title="Order not found" body="This order is not available in the current workspace." />;
  const customer = state.customers.find((item) => item.id === order.customerId);
  const branch = state.branches.find((item) => item.id === order.branchId);
  const paid = orderPaid(state, order.id);
  const balance = orderBalance(state, order);
  const canAdvanceOrder = user.role === 'admin' || (user.role === 'staff' && order.assignedStaffId === user.id);
  const upcoming = canAdvanceOrder ? nextStatus(order.status) : null;
  const savePayment = (event: React.FormEvent) => { event.preventDefault(); const value = Number(amount); if (!value || value <= 0 || value > balance) return; dispatch({ type: 'ADD_PAYMENT', payment: { id: makeId('payment'), orderId: order.id, amount: value, method, paidAt: new Date().toISOString(), receivedByUserId: user.id } }); setAmount(''); };
  return <><PageTitle eyebrow="Order record" title={order.number} description={`${customer?.name} · ${branch?.name}`} actions={<><Link to="/orders"><Button variant="secondary"><ArrowLeft /> Orders</Button></Link><Button variant="secondary" onClick={() => window.print()}><Printer /> Print</Button></>} /><div className="order-detail-grid"><div className="order-detail-main"><Card className="order-overview"><div className="overview-head"><div className="order-large-icon"><Shirt /></div><div><span>Current stage</span><h2>{statusLabels[order.status]}</h2></div><StatusPill status={order.status} /></div><div className="overview-facts"><div><UserRound /><span>Customer<strong>{customer?.name}</strong></span></div><div><MapPin /><span>Branch<strong>{branch?.shortName}</strong></span></div><div><CalendarDays /><span>Expected due<strong>{shortDate(order.dueAt)}</strong></span></div><div><Package2 /><span>Intake<strong>{order.intakeMethod.replaceAll('_', ' ')}</strong></span></div></div>{order.notes ? <div className="order-note"><FileText /><span><strong>Care note</strong>{order.notes}</span></div> : null}</Card><Card className="garment-card"><div className="card-heading"><div><span className="eyebrow">Order contents</span><h2>Garments & services</h2></div><b>{order.items.reduce((sum, item) => sum + item.quantity, 0)} items</b></div>{order.items.map((item) => { const service = state.services.find((entry) => entry.id === item.serviceId); return <div className="garment-row" key={item.id}><span><Shirt /></span><div><strong>{item.description}</strong><small>{service?.name} · {item.quantity} × {money(item.unitPrice)}</small></div><b>{money(item.quantity * item.unitPrice)}</b></div>; })}</Card><Card className="journey-card"><div className="card-heading"><div><span className="eyebrow">Customer visible</span><h2>Care journey</h2></div></div><div className="journey">{statusSequence.map((status, index) => { const event = [...order.events].reverse().find((item) => item.status === status); const reached = Boolean(event) || statusSequence.indexOf(order.status) >= index; return <div className={reached ? 'reached' : ''} key={status}><span>{reached ? <Check /> : index + 1}</span><i /><section><strong>{statusLabels[status]}</strong><small>{event ? dateTime(event.at) : 'Pending'}</small>{event?.note ? <p>{event.note}</p> : null}</section></div>; })}</div></Card></div><aside className="order-detail-side">{user.role !== 'customer' && upcoming ? <Card className="stage-action"><span><ArrowRight /></span><h3>Advance workflow</h3><p>Move this order to <strong>{statusLabels[upcoming]}</strong> and timestamp the customer’s tracker.</p><Button className="full-width" onClick={() => dispatch({ type: 'UPDATE_ORDER_STATUS', orderId: order.id, status: upcoming, userId: user.id })}>Complete current stage <ArrowRight /></Button></Card> : null}<Card className="payment-summary"><span className="eyebrow">Payment</span><h2>Order total</h2><div><span>Subtotal</span><b>{money(orderSubtotal(order))}</b></div>{order.discount ? <div><span>Discount</span><b className="green">−{money(order.discount)}</b></div> : null}{order.deliveryFee ? <div><span>Pickup / delivery</span><b>{money(order.deliveryFee)}</b></div> : null}<div className="payment-total"><span>Total</span><b>{money(orderTotal(order))}</b></div><div><span>Amount paid</span><b className="green">{money(paid)}</b></div><div className="payment-balance"><span>Balance due</span><b>{money(balance)}</b></div>{user.role !== 'customer' && balance > 0 ? <form onSubmit={savePayment} className="payment-form"><FormField label="Record payment"><input required type="number" step="0.01" min="0.01" max={balance} value={amount} onChange={(event) => setAmount(event.target.value)} placeholder="0.00" /></FormField><div className="method-grid">{methods.map((item) => <button type="button" onClick={() => setMethod(item)} className={method === item ? 'selected' : ''} key={item}>{item.replaceAll('_', ' ')}</button>)}</div><Button type="submit" className="full-width"><CreditCard /> Save payment</Button></form> : null}</Card>{paid > 0 ? <Card className="receipt-mini"><FileText /><div><strong>Receipt available</strong><span>{state.payments.filter((item) => item.orderId === order.id).length} payment record(s)</span></div><Button variant="ghost" onClick={() => window.print()}>Print</Button></Card> : null}</aside></div></>;
}
