import { makeId, money, orderTotal, type CustomerMeasurements } from '@gatsi/domain';
import { KeyRound, Mail, Phone, Plus, Ruler, Search, Star, UserPlus } from 'lucide-react';
import { useMemo, useState } from 'react';
import { Button, Card, FormField, PageTitle } from '../components/ui';
import { useAppStore } from '../store/AppStore';

export function CustomersPage() {
  const { state, dispatch } = useAppStore();
  const [query, setQuery] = useState('');
  const [adding, setAdding] = useState(false);
  const [firstName, setFirstName] = useState(''); const [lastName, setLastName] = useState(''); const [phone, setPhone] = useState(''); const [email, setEmail] = useState(''); const [address, setAddress] = useState('');
  const [unit, setUnit] = useState<CustomerMeasurements['unit']>('cm');
  const [measurements, setMeasurements] = useState<Record<string, string>>({});
  const [credentials, setCredentials] = useState<{ username: string; password: string } | null>(null);
  const branchId = state.activeBranchId === 'all' ? state.branches[0].id : state.activeBranchId;
  const customers = useMemo(() => state.customers.filter((customer) => (state.activeBranchId === 'all' || customer.branchId === state.activeBranchId) && `${customer.name} ${customer.phone} ${customer.email}`.toLowerCase().includes(query.toLowerCase())), [state, query]);
  const setMeasurement = (key: string, value: string) => setMeasurements((current) => ({ ...current, [key]: value }));
  const add = (event: React.FormEvent) => {
    event.preventDefault();
    const customerId = makeId('customer'); const username = firstName.trim(); const password = lastName.trim().toUpperCase();
    const values = Object.fromEntries(Object.entries(measurements).filter(([, value]) => value !== '').map(([key, value]) => [key, Number(value)]));
    dispatch({ type: 'CREATE_CUSTOMER', customer: { id: customerId, name: `${firstName.trim()} ${lastName.trim()}`, phone, email, address, joinedAt: new Date().toISOString(), branchId, loyaltyPoints: 0, measurements: { unit, ...values } }, user: { id: makeId('user'), role: 'customer', name: `${firstName.trim()} ${lastName.trim()}`, email, phone, branchIds: [branchId], customerId, avatarColor: '#0EA5A4', username, password } });
    setCredentials({ username, password }); setAdding(false); setFirstName(''); setLastName(''); setPhone(''); setEmail(''); setAddress(''); setMeasurements({});
  };
  const measurementFields = ['height', 'neck', 'chest', 'waist', 'hips', 'shoulder', 'sleeve', 'inseam'];
  return <><PageTitle eyebrow="CRM" title="Customers" description="Order history, measurements, loyalty and lifetime value across every branch." actions={<Button onClick={() => { setAdding(!adding); setCredentials(null); }}><Plus /> Add customer</Button>} />{credentials ? <Card className="credential-notice"><KeyRound /><div><strong>Customer login created</strong><span>Username: <b>{credentials.username}</b> · Password: <b>{credentials.password}</b></span></div><button onClick={() => setCredentials(null)} aria-label="Dismiss">×</button></Card> : null}{adding ? <Card className="inline-form"><div className="card-heading"><div><span className="eyebrow">New record</span><h2>Onboard a customer</h2></div></div><form onSubmit={add} className="form-grid two"><FormField label="First name"><input required value={firstName} onChange={(event) => setFirstName(event.target.value)} /></FormField><FormField label="Last name"><input required value={lastName} onChange={(event) => setLastName(event.target.value)} /></FormField><FormField label="Phone"><input required value={phone} onChange={(event) => setPhone(event.target.value)} /></FormField><FormField label="Email"><input type="email" value={email} onChange={(event) => setEmail(event.target.value)} /></FormField><FormField label="Address"><input value={address} onChange={(event) => setAddress(event.target.value)} /></FormField><FormField label="Measurement unit"><select value={unit} onChange={(event) => setUnit(event.target.value as CustomerMeasurements['unit'])}><option value="cm">Centimetres (cm)</option><option value="in">Inches (in)</option></select></FormField><div className="measurement-section"><div className="measurement-heading"><Ruler /><div><strong>Measurements</strong><small>Optional — add what is available</small></div></div><div className="measurement-grid">{measurementFields.map((field) => <FormField key={field} label={`${field[0].toUpperCase()}${field.slice(1)} (${unit})`}><input type="number" min="0" step="0.1" value={measurements[field] ?? ''} onChange={(event) => setMeasurement(field, event.target.value)} /></FormField>)}</div></div><div className="form-actions"><Button type="button" variant="secondary" onClick={() => setAdding(false)}>Cancel</Button><Button type="submit"><UserPlus /> Save customer</Button></div></form></Card> : null}<div className="toolbar"><label className="search-box"><Search /><input value={query} onChange={(event) => setQuery(event.target.value)} placeholder="Search name, phone or email..." /></label></div><div className="customer-grid">{customers.map((customer) => { const orders = state.orders.filter((order) => order.customerId === customer.id); const spend = orders.reduce((sum, order) => sum + orderTotal(order), 0); const recorded = customer.measurements ? Object.entries(customer.measurements).filter(([key, value]) => key !== 'unit' && value != null) : []; return <Card className="customer-tile" key={customer.id}><div className="customer-card-head"><span>{customer.name.slice(0, 2).toUpperCase()}</span><div><strong>{customer.name}</strong><small>Customer since {new Date(customer.joinedAt).toLocaleDateString([], { month: 'short', year: 'numeric' })}</small></div><i><Star /></i></div><div className="contact-line"><Phone /> {customer.phone}</div><div className="contact-line"><Mail /> {customer.email || 'No email recorded'}</div>{recorded.length ? <div className="measurement-summary"><Ruler /> {recorded.map(([key, value]) => `${key} ${value}${customer.measurements?.unit}`).join(' · ')}</div> : null}<div className="customer-card-stats"><span><b>{orders.length}</b><small>Orders</small></span><span><b>{money(spend)}</b><small>Lifetime value</small></span><span><b>{customer.loyaltyPoints}</b><small>Points</small></span></div></Card>; })}</div></>;
}
